import React from "react";

const DetailPic = (props) => {
    return(
        <div className="detailPic" style={{backgroundColor: `${props.color}`}}>

        </div>
    );
}

export default DetailPic;